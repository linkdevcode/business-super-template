VITE_API_URL=http://localhost:5000
# URL Backend API được publish sau khi deploy (ví dụ: cổng 5000 map vào container backend).

VITE_APP_URL=http://localhost:4173
# URL frontend production (ví dụ khi serve qua Vite preview hoặc Nginx/nginx-like).

